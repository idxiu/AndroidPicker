<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1.0"
        android:gravity="center_horizontal"
        android:orientation="horizontal">

        <com.github.cqrframe.widget.CqrWheelView
            android:id="@+id/option_picker_wheel"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            app:wheel_atmospheric="false"
            app:wheel_curtain="false"
            app:wheel_curved="false"
            app:wheel_cyclic="true"
            app:wheel_indicator="true"
            app:wheel_indicator_color="#80DA7A85"
            app:wheel_indicator_size="1dp"
            app:wheel_item_text_color="#FF999999"
            app:wheel_item_text_size="14sp"
            app:wheel_same_width="true"
            app:wheel_selected_item_text_color="#FF333333"
            app:wheel_visible_item_count="7" />

        <TextView
            android:id="@+id/option_picker_label"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text=""
            android:textColor="#666666"
            android:textSize="14sp"
            android:textStyle="bold" />

    </LinearLayout>

    <LinearLayout
        android:id="@+id/option_picker_button"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/option_picker_cancel"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1.0"
            android:background="#B0B0B0"
            android:gravity="center"
            android:text="取消"
            android:textColor="#FFFFFF"
            android:textSize="15sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/option_picker_confirm"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1.0"
            android:background="#DA7A85"
            android:gravity="center"
            android:text="确定"
            android:textColor="#FFFFFF"
            android:textSize="15sp"
            android:textStyle="bold" />

    </LinearLayout>

</LinearLayout>